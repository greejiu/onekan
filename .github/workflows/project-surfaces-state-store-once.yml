name: Project surfaces state-store migration once

on:
  push:
    branches:
      - refactor/project-surfaces-state-store

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply migration and cache chain
        env:
          CACHE_BASE: origin/main
        run: node scripts/apply-project-surfaces-state-store-migration.mjs
      - name: Validate migration
        run: |
          set -euo pipefail
          node --check js/project-bookmark.js
          node --check js/project-direction-tabs.js
          node --check js/project-status.js
          for test in scripts/*-regression.mjs; do node "$test"; done
          node scripts/cache-buster-regression.mjs --check-diff origin/main
      - name: Commit migration
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html js/*.js css/*.css scripts/*.mjs
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "refactor: 프로젝트 화면 상태 접근을 공용 store로 이전"
          git push origin HEAD:refactor/project-surfaces-state-store
