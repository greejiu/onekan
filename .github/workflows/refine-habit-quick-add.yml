name: Refine habit quick add
on:
  push:
    branches: [main]
    paths:
      - 'scripts/refine-habit-quick-add.py'
      - '.github/workflows/refine-habit-quick-add.yml'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - name: Apply patch
        run: python3 scripts/refine-habit-quick-add.py
      - name: Validate JavaScript
        run: node --check js/unified-workspace.js
      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html js/unified-workspace.js css/unified-workspace.css
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'Refine habit quick add controls'
          git push
