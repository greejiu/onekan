from pathlib import Path

ROOT = Path('.')

def replace_once(text, old, new, label):
    count = text.count(old)
    if count != 1:
        raise SystemExit(f'{label}: expected 1 occurrence, got {count}')
    return text.replace(old, new, 1)

# work-management.js
path = ROOT / 'js/work-management.js'
text = path.read_text()
text = replace_once(text, '''    if (task.projectId && !validProjectIds.has(task.projectId)) {\n      delete task.projectId;\n      if (task.projectPlan) delete task.projectPlan;\n    }''', '''    if (task.projectId && !validProjectIds.has(task.projectId)) {\n      delete task.projectId;\n      if (task.projectPlan) delete task.projectPlan;\n      if (task.projectPlanLinkedExisting) delete task.projectPlanLinkedExisting;\n    }''', 'cleanup linked existing marker')
text = replace_once(text, '''function renderGoalProjects(goalId) {\n  const editor = $("#goalProjectsEditor");\n  const list = $("#goalProjectList");\n  const count = $("#goalProjectCount");\n  if (!editor || !list) return;\n  const projects = goalId ? sorted(state.projects.filter((item) => item.kind === "project" && item.goalId === goalId)) : [];\n  editor.hidden = !goalId;\n  if (!goalId) {\n    list.innerHTML = "";\n    if (count) count.textContent = "";\n    return;\n  }\n  if (count) count.textContent = `${projects.length}개`;\n  list.innerHTML = projects.length ? projects.map((project) => `<div class="uw-goal-project-row" style="--uw-group:${groupOf(project).color}"><span></span><strong>${esc(project.title)}</strong><small>${esc(statusDefs.find((entry) => entry.id === project.status)?.label || "시작 전")}</small></div>`).join("") : '<div class="uw-goal-project-empty">아직 연결된 프로젝트가 없어요.</div>';\n}''', '''function ensureGoalExistingProjectPicker(editor) {\n  let box = $("#goalExistingProjectLink", editor);\n  if (!box) {\n    box = document.createElement("div");\n    box.id = "goalExistingProjectLink";\n    box.className = "uw-existing-link-box";\n    box.innerHTML = '<select id="goalExistingProjectSelect" aria-label="기존 프로젝트 선택"></select><button id="goalExistingProjectLinkBtn" type="button">기존 프로젝트 연결</button>';\n    editor.appendChild(box);\n  }\n  if (!$("#existingLinkPickerStyle")) {\n    const style = document.createElement("style");\n    style.id = "existingLinkPickerStyle";\n    style.textContent = '.uw-existing-link-box{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:6px;margin-top:7px;padding-top:8px;border-top:1px dashed var(--line)}.uw-existing-link-box select{min-width:0;height:32px;padding:4px 7px;border:1px solid var(--line);border-radius:8px;background:#fff;color:var(--text);font:inherit;font-size:10px}.uw-existing-link-box button{min-height:32px;padding:5px 9px;border:1px solid var(--line);border-radius:8px;background:#fff;color:var(--accent-dark);font:inherit;font-size:10px;cursor:pointer}.uw-existing-link-box button:disabled{opacity:.45;cursor:default}@media(max-width:700px){.uw-existing-link-box{grid-template-columns:1fr}.uw-existing-link-box select,.uw-existing-link-box button{height:36px;font-size:12px}}';\n    document.head.appendChild(style);\n  }\n  return box;\n}\n\nfunction renderGoalProjects(goalId) {\n  const editor = $("#goalProjectsEditor");\n  const list = $("#goalProjectList");\n  const count = $("#goalProjectCount");\n  if (!editor || !list) return;\n  const picker = ensureGoalExistingProjectPicker(editor);\n  const projects = goalId ? sorted(state.projects.filter((item) => item.kind === "project" && item.goalId === goalId)) : [];\n  editor.hidden = !goalId;\n  picker.hidden = !goalId;\n  if (!goalId) {\n    list.innerHTML = "";\n    if (count) count.textContent = "";\n    return;\n  }\n  if (count) count.textContent = `${projects.length}개`;\n  list.innerHTML = projects.length ? projects.map((project) => `<div class="uw-goal-project-row" style="--uw-group:${groupOf(project).color}"><span></span><strong>${esc(project.title)}</strong><small>${esc(statusDefs.find((entry) => entry.id === project.status)?.label || "시작 전")}</small></div>`).join("") : '<div class="uw-goal-project-empty">아직 연결된 프로젝트가 없어요.</div>';\n\n  const available = sorted(state.projects.filter((item) => item.kind === "project" && !item.goalId));\n  const select = $("#goalExistingProjectSelect", picker);\n  const button = $("#goalExistingProjectLinkBtn", picker);\n  if (select) select.innerHTML = available.length ? '<option value="">기존 프로젝트 선택…</option>' + available.map((project) => `<option value="${project.id}">${esc(project.title)} · ${esc(statusDefs.find((entry) => entry.id === project.status)?.label || "시작 전")}</option>`).join("") : '<option value="">연결할 프로젝트가 없어요</option>';\n  if (button) button.disabled = !available.length;\n}''', 'goal existing project picker')
text = replace_once(text, '''  $("#goalProjectNewTitle")?.addEventListener("keydown", (event) => {\n    if (event.key !== "Enter") return;\n    event.preventDefault();\n    event.stopPropagation();\n    $("#goalProjectAddBtn")?.click();\n  });\n  $("#convertProjectBtn")?.addEventListener("click", async (event) => {''', '''  $("#goalProjectNewTitle")?.addEventListener("keydown", (event) => {\n    if (event.key !== "Enter") return;\n    event.preventDefault();\n    event.stopPropagation();\n    $("#goalProjectAddBtn")?.click();\n  });\n  document.addEventListener("click", async (event) => {\n    const button = event.target.closest?.("#goalExistingProjectLinkBtn");\n    if (!button) return;\n    event.preventDefault();\n    event.stopPropagation();\n    const goalId = $("#projectKind")?.value === "goal" ? $("#projectId")?.value : "";\n    const projectId = $("#goalExistingProjectSelect")?.value || "";\n    if (!goalId || !projectId) return;\n    button.disabled = true;\n    try {\n      await writeState((current) => {\n        const goal = current.projects.find((item) => item.id === goalId && item.kind === "goal");\n        const project = current.projects.find((item) => item.id === projectId && item.kind === "project" && !item.goalId);\n        if (!goal || !project) return;\n        project.goalId = goalId;\n      });\n      renderGoalProjects(goalId);\n    } finally {\n      button.disabled = false;\n    }\n  }, true);\n  $("#convertProjectBtn")?.addEventListener("click", async (event) => {''', 'wire existing project link')
text = replace_once(text, '''          if (task.projectId !== id) return;\n          delete task.projectId;\n          if (task.projectPlan) delete task.projectPlan;''', '''          if (task.projectId !== id) return;\n          delete task.projectId;\n          if (task.projectPlan) delete task.projectPlan;\n          if (task.projectPlanLinkedExisting) delete task.projectPlanLinkedExisting;''', 'convert linked task cleanup')
path.write_text(text)

# project-planning.js
path = ROOT / 'js/project-planning.js'
text = path.read_text()
text = replace_once(text, '''function renderDialogPlans() {\n  const dialog = $("#projectDialog");\n  const editor = $("#projectPlanEditor");\n  if (!dialog || !editor) return;''', '''function ensureExistingTaskPicker(editor) {\n  let box = $("#projectExistingTaskLink", editor);\n  if (!box) {\n    box = document.createElement("div");\n    box.id = "projectExistingTaskLink";\n    box.className = "uw-existing-link-box";\n    box.innerHTML = '<select id="projectExistingTaskSelect" aria-label="기존 할일 선택"></select><button id="projectExistingTaskLinkBtn" type="button">기존 할일 연결</button>';\n    editor.appendChild(box);\n  }\n  return box;\n}\n\nfunction renderDialogPlans() {\n  const dialog = $("#projectDialog");\n  const editor = $("#projectPlanEditor");\n  if (!dialog || !editor) return;''', 'task picker helper')
text = replace_once(text, '''  const plans = plansFor(project.id);\n  const complete = plans.filter((plan) => plan.done).length;\n  const group = state.eventGroups.find((item) => item.id === project.groupId) || state.eventGroups[0];''', '''  const plans = plansFor(project.id);\n  const complete = plans.filter((plan) => plan.done).length;\n  const group = state.eventGroups.find((item) => item.id === project.groupId) || state.eventGroups[0];''', 'plans anchor')
text = replace_once(text, '''  const list = $("#projectPlanList");\n  if (list) list.innerHTML = plans.length ? plans.map(planRow).join("") : '<div class="uw-project-plan-empty">프로젝트를 실행할 만큼 작게 나눠보세요.</div>';\n}''', '''  const list = $("#projectPlanList");\n  if (list) list.innerHTML = plans.length ? plans.map(planRow).join("") : '<div class="uw-project-plan-empty">프로젝트를 실행할 만큼 작게 나눠보세요.</div>';\n\n  const picker = ensureExistingTaskPicker(editor);\n  const available = [...state.tasks]\n    .filter((task) => !task.projectId && task.projectPlan !== true && task.recurrenceOn !== true)\n    .sort((a, b) => String(a.date || "9999").localeCompare(String(b.date || "9999")) || String(a.title || "").localeCompare(String(b.title || ""), "ko"));\n  const select = $("#projectExistingTaskSelect", picker);\n  const button = $("#projectExistingTaskLinkBtn", picker);\n  if (select) select.innerHTML = available.length ? '<option value="">기존 할일 선택…</option>' + available.map((task) => `<option value="${task.id}">${esc(task.title)} · ${task.date ? esc(task.date) : "언젠가"}${task.done ? " · 완료" : ""}</option>`).join("") : '<option value="">연결할 할일이 없어요</option>';\n  if (button) button.disabled = !available.length;\n}''', 'render existing task options')
text = replace_once(text, '''async function deletePlan(id) {\n  await writeState((current) => {\n    current.tasks = current.tasks.filter((item) => !(item.id === id && item.projectPlan === true));\n    current.timeBlocks = Array.isArray(current.timeBlocks) ? current.timeBlocks.filter((block) => block.taskId !== id) : [];\n  });\n}''', '''async function deletePlan(id) {\n  await writeState((current) => {\n    const task = current.tasks.find((item) => item.id === id && item.projectPlan === true);\n    if (!task) return;\n    if (task.projectPlanLinkedExisting) {\n      delete task.projectId;\n      delete task.projectPlan;\n      delete task.projectPlanLinkedExisting;\n      return;\n    }\n    current.tasks = current.tasks.filter((item) => item.id !== id);\n    current.timeBlocks = Array.isArray(current.timeBlocks) ? current.timeBlocks.filter((block) => block.taskId !== id) : [];\n  });\n}''', 'unlink existing task instead of delete')
text = replace_once(text, '''    const add = event.target.closest?.("#projectPlanAddBtn");\n    if (add) {\n      event.preventDefault();\n      event.stopPropagation();\n      try { await addPlan(); } catch (error) { console.error("프로젝트 계획 추가 실패", error); }\n      return;\n    }\n\n    const toggle = event.target.closest?.("[data-project-plan-toggle]");''', '''    const add = event.target.closest?.("#projectPlanAddBtn");\n    if (add) {\n      event.preventDefault();\n      event.stopPropagation();\n      try { await addPlan(); } catch (error) { console.error("프로젝트 계획 추가 실패", error); }\n      return;\n    }\n\n    const linkExisting = event.target.closest?.("#projectExistingTaskLinkBtn");\n    if (linkExisting) {\n      event.preventDefault();\n      event.stopPropagation();\n      const projectId = $("#projectKind")?.value === "project" ? $("#projectId")?.value : "";\n      const taskId = $("#projectExistingTaskSelect")?.value || "";\n      if (!projectId || !taskId) return;\n      linkExisting.disabled = true;\n      try {\n        await writeState((current) => {\n          const project = current.projects.find((item) => item.id === projectId && item.kind === "project");\n          const task = current.tasks.find((item) => item.id === taskId && !item.projectId && item.projectPlan !== true && item.recurrenceOn !== true);\n          if (!project || !task) return;\n          task.projectId = projectId;\n          task.projectPlan = true;\n          task.projectPlanLinkedExisting = true;\n        });\n      } finally {\n        linkExisting.disabled = false;\n      }\n      return;\n    }\n\n    const toggle = event.target.closest?.("[data-project-plan-toggle]");''', 'wire existing task link')
path.write_text(text)

# index cache bust
path = ROOT / 'index.html'
text = path.read_text()
text = replace_once(text, 'js/work-management.js?v=11', 'js/work-management.js?v=12', 'work management cache')
text = replace_once(text, 'js/project-planning.js?v=2', 'js/project-planning.js?v=3', 'project planning cache')
path.write_text(text)
